# SysToy 环境自检清单

## 1. 使用说明

本清单用于学生团队在正式进入开发前进行环境自检。

## 2. 基本信息

- 团队名称：
- 检查日期：
- 检查人：

## 3. 自检项目

| 编号 | 检查项 | 结果 | 备注 |
|---|---|---|---|
| 1 | Windows 10 环境可用 | 通过 / 不通过 |  |
| 2 | WSL2 可正常进入 | 通过 / 不通过 |  |
| 3 | `nasm` 可执行 | 通过 / 不通过 |  |
| 4 | `gcc` 可执行 | 通过 / 不通过 |  |
| 5 | `make` 可执行 | 通过 / 不通过 |  |
| 6 | `qemu` 可执行 | 通过 / 不通过 |  |
| 7 | 构建命令可执行 | 通过 / 不通过 |  |
| 8 | 基线镜像可生成 | 通过 / 不通过 |  |
| 9 | QEMU 可加载基线镜像 | 通过 / 不通过 |  |
| 10 | 关键输出可观察 | 通过 / 不通过 |  |

## 4. 建议检查命令

建议按本机实际环境执行等价命令：

1. `wsl --status`
2. `nasm -v`
3. `gcc --version`
4. `make --version`
5. `qemu-system-i386 --version`

## 5. 常见失败类型

1. 工具不存在
2. 工具存在但无法执行
3. QEMU 命令不正确
4. 镜像未生成
5. 启动后无输出

## 6. 自检结论

- 是否通过：是 / 否
- 说明：

## 7. 未通过项整改

1. 
2. 
3. 
